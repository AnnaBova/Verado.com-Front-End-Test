import { MessageMarkerDirective } from './message-marker.directive';

describe('MessageMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
